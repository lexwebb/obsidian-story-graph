import React from "react";

import StoryCard from "./StoryCard";

type Props = {
  cards: Card[][];
};

const StoryColumns = (props: Props) => {
  return <div>StoryColumns</div>;
};

export default StoryColumns;
